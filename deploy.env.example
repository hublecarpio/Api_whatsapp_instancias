# ============================================
# WhatsApp SaaS - Deploy Environment Variables
# ============================================
# Copy this file and fill in your values
# Usage: docker stack deploy -c docker-stack-external-db.yml --env-file deploy.env whatsapp

# ============================================
# Database (YOUR EXTERNAL POSTGRESQL)
# ============================================
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:tu-password@host.docker.internal:5432/whatsapp_saas

# NOTE: If your PostgreSQL is running:
# - On the same Docker host: use host.docker.internal (Linux may need extra config)
# - On another server: use the server IP or hostname
# - In another Docker container: use the container name and shared network

# ============================================
# Authentication
# ============================================
SESSION_SECRET=cambia-esto-por-un-string-largo-de-32-caracteres-minimo

# ============================================
# MinIO / S3 Storage
# ============================================
MINIO_ENDPOINT=https://tu-minio.com
MINIO_ACCESS_KEY=tu-access-key
MINIO_SECRET_KEY=tu-secret-key
MINIO_BUCKET=whatsapp-media
MINIO_PUBLIC_URL=https://tu-minio-publico.com
